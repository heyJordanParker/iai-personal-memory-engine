[build-system]
requires      = ["maturin>=1,<2"]
build-backend = "maturin"

[project]
name    = "iai-mcp-native"
version = "0.1.0"
requires-python = ">=3.11"

[tool.maturin]
module-name         = "iai_mcp_native"
features            = ["extension-module"]
no-default-features = true
